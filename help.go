package main

import "fmt"

const FLAG_Q_DESC = "silent mode, there is no output of information about the operation of the program"
const FLAG_CONF_DESC = "full path to configuration file"

func common() {
	fmt.Println("s4sm", "Service For Server Maintenance")
	// todo
}
